commands for Makefile:

make - Creates static library without list - related funcs;
make clean - removes object files;
make fclean - removes all files (including libft.a);
make bonus - creates static library which will contain all functions;
make re - fclean + make.
